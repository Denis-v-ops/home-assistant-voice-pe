packages:
  device: !include ../../home-assistant-voice.yaml

substitutions:
  nova_gateway_url: ws://voice-gateway.invalid:8080/v1/device
  nova_device_id: ci-validation-device

wifi:
  ssid: ci-validation-network
  password: ci-validation-password
